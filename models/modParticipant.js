const Participant = require("./schema/participants.schema");

module.exports = {
	makeParticipant: async person => {
		/**
		 * 참가자를 DB에 넣는 작업
		 */
	},
	checkOverlap: async person => {
		/**
		 * 중복 참여 체크
		 */
	}
};
